/* ---------------------------------
Profile
--------------------------------- */

import React, { ReactElement } from "react";
import Layout from "../../components/Layout/Layout";

interface IOwnProps {}

export default function Profile(props: IOwnProps): ReactElement {
  return <Layout root="Profile">profile</Layout>;
}

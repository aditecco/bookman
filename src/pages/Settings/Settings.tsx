/* ---------------------------------
Settings
--------------------------------- */

import React, { ReactElement } from "react";
import Layout from "../../components/Layout/Layout";

interface IOwnProps {}

export default function Settings(props: IOwnProps): ReactElement {
  return <Layout root="Settings">Settings</Layout>;
}
